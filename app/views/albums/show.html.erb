<div id="albums">
  <%= render @album.photos %>
</div>

<p>
  <%= link_to "Add a photo", new_photo_path(:album_id => @album) %> |
  <%= link_to "Remove Album", @album, data: {:confirm => 'Are you sure?'}, :method => :delete %> |
  <%= link_to "View Albums", albums_path %>
</p>